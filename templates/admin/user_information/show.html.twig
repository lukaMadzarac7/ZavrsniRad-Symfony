{% extends 'admin/base.html.twig' %}

{% block title %}UserInformation{% endblock %}

{% block body %}
    <h1>UserInformation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_information.id }}</td>
            </tr>
            <tr>
                <th>User</th>
                <td>{{ user_information.user.name ~ " " ~ user_information.user.surname }} </td>
            </tr>
            <tr>
                <th>Phone_number</th>
                <td>{{ user_information.phoneNumber }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ user_information.adress }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ user_information.city.city }}</td>
            </tr>
            <tr>
                <th>County</th>
                <td>{{ user_information.county.county }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ user_information.country.country }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ user_information.createdAt ? user_information.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ user_information.updatedAt ? user_information.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_information_index') }}">back to list</a>

    <a href="{{ path('app_user_information_edit', {'id': user_information.id}) }}">edit</a>

    {{ include('admin/user_information/_delete_form.html.twig') }}
{% endblock %}
